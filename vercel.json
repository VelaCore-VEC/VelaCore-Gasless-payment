{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/relay",            "destination": "/api/relay" },
    { "source": "/api/relay/(.*)",       "destination": "/api/relay" }
  ]
}